WHOOP_CLIENT_ID=
WHOOP_CLIENT_SECRET=
# Obtain once via "Connect WHOOP" in the dashboard (OAuth). WHOOP rotates refresh tokens; persist the latest or re-connect after server resets.
WHOOP_REFRESH_TOKEN=
AWS_S3_BUCKET=
BACKEND_BASE_URL=