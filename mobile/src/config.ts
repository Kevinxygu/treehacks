/**
 * Runtime config (not inlined by Babel). If voice chat fails with "Network request failed",
 * set your Mac's IP here and ensure the device/simulator is on the same network.
 * Find your IP: System Settings → Network, or run: ipconfig getifaddr en0
 */
export const DEV_API_HOST = "10.19.176.35";
export const AGENT_PORT = "3001";
export const FASTAPI_PORT = "8000";
